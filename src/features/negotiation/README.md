# Negotiation feature

Person 2 owns this folder.

Build against fixture `GroupContext` objects. The engine should not know whether context came from XTrace or the mock provider.

Both "Priya joined" and "Jordan revised a belief" use the same rebalance operation.

