# Memory feature

Person 1 owns this folder.

Start with a deterministic mock implementation of `MemoryGateway`. Add the XTrace adapter only after write, recall, contradiction, and revision-history behavior are verified.

Do not expose provider-specific response shapes outside this folder.

